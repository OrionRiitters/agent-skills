# Extensions

Shared pi TypeScript extensions live here.

Extensions can add tools, commands, event hooks, UI, model/provider behavior, and other runtime functionality. Review extension code before enabling it because extensions run with local user permissions.
