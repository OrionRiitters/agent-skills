# Prompt Templates

Shared pi prompt templates live here as Markdown files.

Prompt templates are reusable slash-command prompts that can be loaded from this repository as a pi package or referenced from `settings.json`.
