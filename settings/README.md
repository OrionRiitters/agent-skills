# Settings

Shared pi settings snippets and examples live here.

Do not commit secrets, OAuth tokens, API keys, or machine-local credentials. Prefer examples and documented snippets over personal `settings.json` files with absolute paths.
